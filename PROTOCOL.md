This document is just temporary to document wireshark findings.
A proper and more readable document will (might) be created in the future.

## Observations

-   UDP with (generally) the port `34197`.

## Protocol

### ConnectionRequestRequest1
